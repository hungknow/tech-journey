class Solution:
    def native_solution(self, s: str, p: str) -> bool:
        # s_cur_index = 0
        # p_cur_index = 0
        # iterate until s_cur_index is greater than the length of the string
            # if the current character is a dot, increase s_cur_index by 1
            # if the current character is a star, increase s_cur_index by 1 as long as s[s_cur_index] is equal to p[s_cur_index - 1]
        pass