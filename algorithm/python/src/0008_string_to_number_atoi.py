class Solution:
    def native_solution(self, s: str) -> int:
        # Trim the leading and trailing spaces
        # ans = 0
        # detect if the first character is a sign
        # iterate through the string
            # If the character isn't a digit, break
            # Convert the character to a number
            # ans = ans * 10 + number
            # if ans is greater than the max int, return the max int
            # if ans is less than the min int, return the min int
        # return ans
        pass
