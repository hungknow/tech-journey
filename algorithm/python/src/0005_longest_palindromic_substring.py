class Solution:
    def native_solution(self, s: str) -> str:
        ans_left = 0
        ans_right = 0
        def extend(s: str, left: int, right: int) -> (int, int):
            pass
        
        # current_index = iterate from [0..len(s)-1],
            # For odd length palindrome
            # (left, right) = extend(s, current_index, current_index)
            # if the new left and right are greater than the current ans_left and ans_right, update the ans_left and ans_right
            
            # For even length palindrome
            # Detect it's even length palindrome
                # if 
            
        return (ans_left, ans_right)

    def manacher_solution(self, s: str) -> str:
        pass

    def string_hashing(self, s: str) -> str:
        pass

    def suffix_trees(self, s: str) -> str:
        pass

    def fast_lca(self, s: str) -> str:
        pass