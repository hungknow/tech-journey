class Solution:
    def native_solution(self, s: str, numRows: int) -> str:
        # k = 0
        # rows = [""] * numRows
        # iterate the characters in the string
            # append the current character to the row at the index k
            # Detect if it's the first row or last row
            #   direction *= -1
            # k += direction
        pass